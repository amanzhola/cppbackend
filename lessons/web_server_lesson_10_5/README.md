# 🚀 Практическое занятие №3

## Обрабатываем разные маршруты

---

# 📌 Кратко

| 📌 Что это                                             | 📖 Описание                                                                                                                                                                                                                                                     |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Сервер с маршрутизацией по URL и проверкой HTTP-метода | В этой работе сервер анализирует `req.target()` и `req.method()`, различает пути `/`, `/hello`, `/time`, возвращает разные HTTP-статусы `200 OK`, `404 Not Found`, `405 Method Not Allowed` и формирует корректный HTTP-ответ через функцию `MakeResponse(...)` |

---

# 🎯 Цель

| 🎯 Что нужно понять                                                                                                         | 📖 Что это означает на практике                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Извлекать смысл из `req.target()`, различать ресурсы по пути, возвращать разные статус-коды и строить корректный HTTP-ответ | Сервер должен понимать, какой именно URL запросил клиент, отличать корректные маршруты от неизвестных, отдельно обрабатывать неподдерживаемые методы вроде `POST`, выставлять правильный статус ответа и возвращать понятное содержимое в `body` |

---

# 🧠 Главная идея урока

| 🧠 Ключевая логика                                                                                                                                                                                        | 📖 Смысл                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cpp<br>if (req.method() != http::verb::get) { ... }<br>else if (req.target() == "/") { ... }<br>else if (req.target() == "/hello") { ... }<br>else if (req.target() == "/time") { ... }<br>else { ... }` | Сервер последовательно проверяет HTTP-метод и путь запроса, после чего выбирает правильный текст ответа и правильный код состояния: `200` для известных маршрутов, `404` для неизвестного пути и `405` для неподдерживаемого метода |

---

# ⚙️ Основной код маршрутизации

```cpp
http::response<http::string_body>
MakeResponse(const http::request<http::string_body>& req) {
    http::response<http::string_body> res;
    res.version(req.version());
    res.set(http::field::server, "Lesson Server");
    res.set(http::field::content_type, "text/plain; charset=utf-8");
    res.keep_alive(req.keep_alive());

    if (req.method() != http::verb::get) {
        res.result(http::status::method_not_allowed);
        res.body() = "Only GET method is supported\n";
    } else if (req.target() == "/") {
        res.result(http::status::ok);
        res.body() = "Main page\n";
    } else if (req.target() == "/hello") {
        res.result(http::status::ok);
        res.body() = "Hello!\n";
    } else if (req.target() == "/time") {
        res.result(http::status::ok);
        res.body() = "This could be current server time\n";
    } else {
        res.result(http::status::not_found);
        res.body() = "404 Not Found\n";
    }

    res.content_length(res.body().size());
    return res;
}
```

---

# 🌐 Маршруты и ответы

| 🌐 Запрос      | 📥 Ответ сервера                                                                                                                               |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /`        | Сервер возвращает статус `200 OK` и текст `Main page`, потому что корневой путь считается главным маршрутом приложения                         |
| `GET /hello`   | Сервер возвращает статус `200 OK` и текст `Hello!`, потому что это отдельный поддерживаемый маршрут                                            |
| `GET /time`    | Сервер возвращает статус `200 OK` и текст `This could be current server time`, потому что путь `/time` явно описан в маршрутизации             |
| `GET /unknown` | Сервер возвращает статус `404 Not Found`, потому что такой путь не поддерживается                                                              |
| `POST /hello`  | Сервер возвращает статус `405 Method Not Allowed` и текст `Only GET method is supported`, потому что в этой работе разрешён только метод `GET` |

---

# 🔍 Проверка и результат

| 🔍 Проверка (curl)                                                                                                                                                                             | 📥 Результат                                                                                                        |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `bash<br>curl http://localhost:8080/<br>curl http://localhost:8080/hello<br>curl http://localhost:8080/time<br>curl http://localhost:8080/unknown<br>curl -X POST http://localhost:8080/hello` | `text<br>Main page<br>Hello!<br>This could be current server time<br>404 Not Found<br>Only GET method is supported` |

---

# 🖥 Логи сервера и смысл этих логов

| 🖥 Что видно в терминале сервера                                                                                                                                             | 🌐 Что это доказывает                                                                                                                                                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `text<br>Server started on 0.0.0.0:8080<br>Client connected<br>Request: GET /<br>Request: GET /hello<br>Request: GET /time<br>Request: GET /unknown<br>Request: POST /hello` | `text<br>сервер действительно читает путь запроса через req.target()<br>сервер различает HTTP-методы через req.method()<br>сервер возвращает разные ответы для разных ресурсов<br>сервер умеет отделять успешные маршруты от ошибок 404 и 405` |

---

# 🌐 Пример HTTP-смысла ответа

| 🌐 Что получает клиент                                                                                                                                             | 📖 Почему это правильно                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Для известных маршрутов клиент получает `200 OK`, для неизвестного пути получает `404 Not Found`, а для неподдерживаемого метода получает `405 Method Not Allowed` | Это и есть корректное поведение HTTP-сервера: ответ должен зависеть не только от тела запроса, но и от URL, и от метода, чтобы клиент получал честный и понятный результат взаимодействия с сервером |

---

# 🚀 Как собрать и запустить

| 🚀 Что сделать                                              | 💻 Команды                                                                                                                                                                             |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Перейти в папку проекта, пересобрать его и запустить сервер | `bash<br>cd ~/cppbackend/lessons/web_server_lesson_10_5<br>rm -rf build<br>mkdir build<br>cd build<br>conan install .. --build=missing<br>cmake ..<br>cmake --build .<br>./bin/server` |

---

# 🧪 Полная инструкция проверки

| 📋 Что сделать                                                                                                                                                                                                                                                                                                                                                             | ✅ Как понять, что всё правильно                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `text<br>1. Запустить сервер в первом терминале<br>2. Открыть второй терминал<br>3. Выполнить curl http://localhost:8080/<br>4. Выполнить curl http://localhost:8080/hello<br>5. Выполнить curl http://localhost:8080/time<br>6. Выполнить curl http://localhost:8080/unknown<br>7. Выполнить curl -X POST http://localhost:8080/hello<br>8. Сравнить ответы с ожидаемыми` | `text<br>для / сервер возвращает Main page<br>для /hello сервер возвращает Hello!<br>для /time сервер возвращает This could be current server time<br>для неизвестного пути сервер возвращает 404 Not Found<br>для POST сервер возвращает Only GET method is supported<br>значит маршрутизация и статус-коды реализованы правильно` |

---

# ✅ Формулировка проверки

| ✅ Проверка                                                                                                                                                                                                                                | ✅ Условие выполнено                                                                                                                        |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Запустить сервер. Проверить несколько разных маршрутов и неподдерживаемый метод через `curl`. Если сервер различает URL, возвращает разные тексты и выставляет правильные статус-коды `200`, `404`, `405`, то цель упражнения достигнута. | Да, если ответ зависит от `req.target()` и `req.method()`, а сервер корректно формирует содержимое ответа и HTTP-статус для каждого случая |

---

# ⚠️ Что будет, если маршрутизацию не сделать

| ❌ Неправильный вариант                                                                                                            | 💥 Что сломается                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Если не проверять `req.target()` и всегда возвращать один и тот же ответ, либо не проверять `req.method()` и принимать всё подряд | Сервер перестанет различать ресурсы по пути, не сможет корректно отвечать на неизвестные маршруты, будет возвращать неверные HTTP-статусы и не покажет главную идею урока про полноценную обработку разных маршрутов |

---

# 🏗 Архитектура

| 🏗 Компоненты                                                                                                                | 📖 Роль каждого компонента                                                                                                                                                                                                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `http::request<http::string_body>`, `req.target()`, `req.method()`, `MakeResponse(...)`, `http::response<http::string_body>` | Объект запроса содержит путь и метод; `req.target()` сообщает, какой ресурс запросил клиент; `req.method()` сообщает, какой HTTP-метод использован; `MakeResponse(...)` выбирает правильную ветку логики; объект ответа хранит статус, заголовки и текст ответа, который затем отправляется клиенту |

---

# 🏁 Итог

| 🏁 Что доказано этой работой                                                                                                                                                                                      | 📌 Вывод                                                                                                                                                                                                |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Сервер умеет анализировать URL и метод запроса, различать маршруты `/`, `/hello`, `/time`, возвращать `404` для неизвестного пути и `405` для неподдерживаемого метода, а также формировать корректный HTTP-ответ | Это уже не просто сервер, который отвечает одинаково на всё, а полноценная основа маршрутизации: клиент обращается к разным ресурсам, а сервер осмысленно выбирает нужный ответ и правильный статус-код |
