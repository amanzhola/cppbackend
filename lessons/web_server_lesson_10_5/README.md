# 🚀 Практическое занятие №3

## Обрабатываем разные маршруты

---

# 📌 Кратко

| 📌 Что это                                             | 📖 Полное описание                                                                                                                                                                                                                                              |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Сервер с маршрутизацией по URL и проверкой HTTP-метода | В этой работе сервер анализирует `req.target()` и `req.method()`, различает пути `/`, `/hello`, `/time`, возвращает разные HTTP-статусы `200 OK`, `404 Not Found`, `405 Method Not Allowed` и формирует корректный HTTP-ответ через функцию `MakeResponse(...)` |

---

# 🎯 Цель

| 🎯 Что нужно понять                                                                                                     | 📖 Что это означает на практике                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Извлекать смысл из req.target, различать ресурсы по пути, возвращать разные статус-коды и строить корректный HTTP-ответ | Сервер должен понимать, какой именно URL запросил клиент, отличать корректные маршруты от неизвестных, отдельно обрабатывать неподдерживаемые методы вроде POST, выставлять правильный статус ответа и возвращать понятное содержимое в body |

---

# 🧠 Главная идея

| 🧠 Ключевая логика                                                                                                                                                                                                                    | 📖 Смысл                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `if (req.method() != http::verb::get) { ... } else if (req.target() == "/") { ... } else if (req.target() == "/hello") { ... } else if (req.target() == "/time") { ... } else { ... } // последовательная проверка метода и маршрута` | Сервер последовательно проверяет HTTP-метод и путь запроса, после чего выбирает правильный текст ответа и правильный код состояния: 200 для известных маршрутов, 404 для неизвестного пути и 405 для неподдерживаемого метода |

---

# ⚙️ Основной код маршрутизации

```cpp
http::response<http::string_body>
MakeResponse(const http::request<http::string_body>& req) {
    http::response<http::string_body> res;
    res.version(req.version());
    res.set(http::field::server, "Lesson Server");
    res.set(http::field::content_type, "text/plain; charset=utf-8");
    res.keep_alive(req.keep_alive());

    if (req.method() != http::verb::get) {
        res.result(http::status::method_not_allowed);
        res.body() = "Only GET method is supported\n";
    } else if (req.target() == "/") {
        res.result(http::status::ok);
        res.body() = "Main page\n";
    } else if (req.target() == "/hello") {
        res.result(http::status::ok);
        res.body() = "Hello!\n";
    } else if (req.target() == "/time") {
        res.result(http::status::ok);
        res.body() = "This could be current server time\n";
    } else {
        res.result(http::status::not_found);
        res.body() = "404 Not Found\n";
    }

    res.content_length(res.body().size());
    return res;
}
```

---

# 🌐 Маршруты и ответы

| 🌐 Запрос    | 📖 Что делает сервер                                                                                     |
| ------------ | -------------------------------------------------------------------------------------------------------- |
| GET /        | Сервер возвращает статус 200 OK и текст Main page, потому что корневой путь считается главным маршрутом  |
| GET /hello   | Сервер возвращает статус 200 OK и текст Hello, потому что это отдельный поддерживаемый маршрут           |
| GET /time    | Сервер возвращает статус 200 OK и текст This could be current server time, потому что маршрут явно задан |
| GET /unknown | Сервер возвращает статус 404 Not Found, потому что путь не существует                                    |
| POST /hello  | Сервер возвращает статус 405 Method Not Allowed, потому что разрешён только GET                          |

---

# 🔍 Проверка и результат

| 🔍 Проверка (curl)                                                                                                                                                                 | 📥 Результат                                                                                                                       |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `curl http://localhost:8080/ ; curl http://localhost:8080/hello ; curl http://localhost:8080/time ; curl http://localhost:8080/unknown ; curl -X POST http://localhost:8080/hello` | Сервер возвращает Main page, Hello, This could be current server time, 404 Not Found и Only GET method is supported соответственно |

---

# 🖥 Логи сервера

| 🖥 Что видно                                                                                                                           | 📖 Что это доказывает                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Server started, Client connected, Request: GET /, Request: GET /hello, Request: GET /time, Request: GET /unknown, Request: POST /hello | Сервер читает путь запроса через req.target, различает HTTP-методы через req.method, возвращает разные ответы и корректно обрабатывает маршруты и ошибки |

---

# 🌐 HTTP-смысл

| 🌐 Что получает клиент                                                        | 📖 Почему это важно                                                               |
| ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Клиент получает разные ответы и статусы в зависимости от URL и метода запроса | Это соответствует стандарту HTTP: сервер должен учитывать и путь, и метод запроса |

---

# 🚀 Сборка и запуск

| 🚀 Что сделать                                          | 💻 Команды                                                                                                                                                                     |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Перейти в папку проекта, пересобрать и запустить сервер | `cd ~/cppbackend/lessons/web_server_lesson_10_5 && rm -rf build && mkdir build && cd build && conan install .. --build=missing && cmake .. && cmake --build . && ./bin/server` |

---

# 🧪 Полная инструкция проверки

| 📋 Что сделать                                                                             | ✅ Как понять, что всё правильно                                                                                          |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Запустить сервер, выполнить curl для разных маршрутов и метода POST, затем сравнить ответы | Сервер возвращает корректные ответы для каждого маршрута и метода, значит маршрутизация и статус-коды работают правильно |

---

# ✅ Формулировка проверки

| ✅ Проверка                                                                  | ✅ Условие выполнено                                               |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| Запустить сервер и выполнить несколько запросов с разными путями и методами | Сервер различает URL и метод и возвращает корректные HTTP-статусы |

---

# ⚠️ Что будет без маршрутизации

| ❌ Ошибка                | 💥 Последствие                      |
| ----------------------- | ----------------------------------- |
| Нет проверки req.target | сервер всегда возвращает один ответ |
| Нет проверки метода     | POST работает неправильно           |
| Нет статусов            | клиент не понимает результат        |

---

# 🏗 Архитектура

| 🏗 Компоненты | 📖 Роль               |
| ------------- | --------------------- |
| request       | содержит путь и метод |
| req.target    | определяет маршрут    |
| req.method    | определяет метод      |
| MakeResponse  | формирует ответ       |
| response      | отправляется клиенту  |

---

# 🏁 Итог

| 🏁 Что доказано                                                              | 📌 Вывод                                                                       |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| Сервер умеет различать маршруты и методы и возвращать корректные HTTP-ответы | Это основа backend: сервер должен понимать запрос и правильно на него отвечать |
